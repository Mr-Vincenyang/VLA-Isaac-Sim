# VLA Platform - 完整依赖列表
# ================================================
# 安装方式: pip install -r requirements.txt
# 
# 注意事项:
# 1. Isaac Sim 用户请在 Isaac Sim Python 环境中安装
# 2. 如果仅使用远程推理，可以跳过 Isaac Sim 相关依赖
# 3. OpenVLA 模型需要特定版本的 transformers 和 timm，已在此文件中固定。
# ================================================

# ============== 核心依赖 ==============
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
pyyaml>=6.0
Pillow>=9.0.0
dataclasses-json>=0.6.0

# ============== 深度学习框架 ==============
# PyTorch - 根据CUDA版本选择
# CPU版本：
# torch>=2.1.0
# CUDA 11.8:
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
# 安装命令: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# ============== Transformers & 模型 ==============
# OpenVLA 要求 transformers==4.40.1（避免 5.x 的 API 变更）
transformers==4.40.1
accelerate>=0.25.0
safetensors>=0.4.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
huggingface-hub>=0.20.0
# OpenVLA 需要 timm 0.9.x 版本
timm>=0.9.10,<1.0.0

# ============== 高效微调 ==============
peft>=0.7.0                    # LoRA, QLoRA等高效微调方法
bitsandbytes>=0.41.0           # INT4/INT8量化
# deepspeed>=0.12.0            # 可选：分布式训练

# ============== 强化学习 ==============
gymnasium>=0.29.0              # OpenAI Gym升级版
stable-baselines3>=2.2.0       # PPO等RL算法
# sb3-contrib>=2.2.0           # 额外RL算法

# ============== 数据处理 ==============
h5py>=3.10.0                   # HDF5数据存储
pandas>=2.0.0                  # 数据分析
datasets>=2.14.0               # HuggingFace datasets
opencv-python>=4.8.0           # 图像处理
imageio>=2.31.0                # 图像/视频读写
imageio-ffmpeg>=0.4.8          # 视频编解码

# ============== 网络通信 ==============
requests>=2.28.0
aiohttp>=3.9.0                 # 异步HTTP
flask>=3.0.0                   # REST API服务
flask-cors>=4.0.0              # CORS支持
grpcio>=1.59.0                 # gRPC通信
grpcio-tools>=1.59.0           # gRPC proto编译
protobuf>=4.24.0               # Protocol Buffers

# ============== 可视化和日志 ==============
tqdm>=4.66.0                   # 进度条
matplotlib>=3.8.0              # 绑图
seaborn>=0.13.0                # 统计可视化
tensorboard>=2.15.0            # 训练可视化
wandb>=0.16.0                  # 实验追踪 (可选，需注册账号)

# ============== 机器人学工具 ==============
spatialmath-python>=1.1.0      # 空间数学（SE3, SO3等）
# roboticstoolbox-python>=1.1.0  # 机器人学工具（可选）

# ============== 开发和测试 ==============
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
black>=23.12.0                 # 代码格式化
isort>=5.13.0                  # import排序
flake8>=7.0.0                  # 代码检查
mypy>=1.8.0                    # 类型检查

# ============== 工具类 ==============
python-dotenv>=1.0.0           # 环境变量管理
click>=8.1.0                   # CLI工具
rich>=13.7.0                   # 美化终端输出
loguru>=0.7.0                  # 更好的日志

# ============== Jupyter支持 ==============
jupyter>=1.0.0
jupyterlab>=4.0.0
ipywidgets>=8.1.0

# ================================================
# Isaac Sim 相关说明
# ================================================
# 以下包由 Isaac Sim / Isaac Lab 环境自动提供，无需手动安装：
#   - omni.isaac.core
#   - omni.isaac.franka  
#   - omni.isaac.sensor
#   - omni.isaac.gym
#   - omni.replicator.core
#
# Isaac Sim 安装指南:
#   1. 下载 NVIDIA Omniverse Launcher
#   2. 通过 Launcher 安装 Isaac Sim 2023.1.1+
#   3. 安装 Isaac Lab: https://isaac-sim.github.io/IsaacLab/
#
# 在 Isaac Sim Python 环境中安装本项目依赖:
#   ${ISAAC_SIM_PATH}/python.sh -m pip install -r requirements.txt
# ================================================

# ================================================
# 可选依赖 (按需取消注释)
# ================================================
# open3d>=0.18.0               # 点云处理
# trimesh>=4.0.0               # 网格处理
# pybullet>=3.2.6              # 替代仿真器
# mujoco>=3.0.0                # MuJoCo仿真
# dm-control>=1.0.0            # DeepMind Control Suite